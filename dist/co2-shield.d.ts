export default class CO2Shield {
  private formContainerId;
  private static ratingDetails;
  constructor(formContainerId: string);
  initialize(): void;
  private createForm;
  private generateShield;
  private validateUrl;
  private fetchCO2Data;
  private updateUI;
  private copy;
  private reset;
}
